{
  "name": "@mana-ssh/vt",
  "version": "0.0.1",
  "private": true,
  "sideEffects": false,
  "main": "./src/index.ts",
  "source": "./src/index.ts",
  "types": "./src/types.ts",
  "files": [
    "dist/**"
  ],
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "lint": "biome lint .",
    "lint:fix": "biome lint --apply ."
  },
  "devDependencies": {
    "@vitest/coverage-istanbul": "^0.34.6",
    "fast-check": "^3.18.0",
    "typescript": "^5.0.4",
    "vitest": "^0.34.6"
  }
}
