{
  "name": "@mana/websocket",
  "version": "0.0.0",
  "private": true,
  "description": "WebSocket transport primitives for Mana SSH sessions.",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts",
      "require": "./src/index.ts"
    }
  },
  "scripts": {
    "clean": "git clean -xfd .",
    "build": "echo 'TODO: implement build step for @mana/websocket'",
    "test": "bun run test:unit && bun run test:e2e",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest --watch",
    "test:unit:coverage": "vitest run --coverage --reporter=dot --silent",
    "test:e2e": "playwright test",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ui": "playwright test --ui",
    "lint": "biome lint .",
    "lint:fix": "biome lint --fix . && biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "sideEffects": false,
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
