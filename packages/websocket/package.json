{
  "name": "@nimbus/websocket",
  "version": "0.0.0",
  "private": true,
  "description": "WebSocket transport primitives for Nimbus SSH sessions.",
  "files": [
    "dist/**"
  ],
  "exports": {
    ".": null,
    "./client/node": {
      "types": "./src/client/node/index.ts",
      "source": "./src/client/node/index.ts",
      "import": "./dist/client/node.js",
      "require": "./dist/client/node.cjs"
    },
    "./client/web": {
      "types": "./src/client/web/index.ts",
      "source": "./src/client/web/index.ts",
      "import": "./dist/client/web.js",
      "require": "./dist/client/web.cjs"
    },
    "./server/node": {
      "types": "./src/server/node/index.ts",
      "source": "./src/server/node/index.ts",
      "import": "./dist/server/node.js",
      "require": "./dist/server/node.cjs"
    }
  },
  "scripts": {
    "clean": "git clean -xfd .",
    "clean:artifacts": "rm -rf dist",
    "build": "npm run clean:artifacts && npm run build:protocol && npm run build:client-web && npm run build:client-web:iife && npm run build:client-node && npm run build:server-node",
    "build:protocol": "vite build --config vite.config.protocol.ts",
    "build:client-web": "vite build --config vite.config.client-web.ts",
    "build:client-web:iife": "vite build --config vite.config.client-web-iife.ts",
    "build:client-node": "vite build --config vite.config.client-node.ts",
    "build:server-node": "vite build --config vite.config.server-node.ts",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "vitest run",
    "test:unit:watch": "vitest --watch",
    "test:unit:coverage": "vitest run --coverage --reporter=dot --silent",
    "test:e2e": "playwright test",
    "test:e2e:debug": "playwright test --debug",
    "test:e2e:ui": "playwright test --ui",
    "lint": "biome lint .",
    "lint:fix": "biome lint --fix . && biome format --write .",
    "typecheck": "tsc --noEmit"
  },
  "sideEffects": false,
  "dependencies": {
    "@nimbus/ssh": "^0.0.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.1",
    "@types/node": "^24.6.1",
    "typescript": "^5.9.2",
    "vite": "^7.1.9",
    "vite-plugin-dts": "^4.5.4",
    "vitest": "^3.2.4"
  }
}
