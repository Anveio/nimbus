/**
 * Placeholder for the future Deno websocket server runtime.
 * Intentionally throws so downstream consumers avoid depending on the surface ahead of its implementation.
 */
export const createDenoWebSocketServer = (): never => {
  throw new Error('Nimbus WebSocket server for Deno is not implemented yet.')
}

