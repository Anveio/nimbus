/**
 * Placeholder for the future Bun websocket server runtime.
 * Intentionally throws so downstream consumers avoid depending on the surface ahead of its implementation.
 */
export const createBunWebSocketServer = (): never => {
  throw new Error('Nimbus WebSocket server for Bun is not implemented yet.')
}

